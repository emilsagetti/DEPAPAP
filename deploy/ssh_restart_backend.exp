#!/usr/bin/expect -f

set timeout 20
set host "31.31.196.161"
set user "u3390483"
set password "Butaeva1984"

spawn ssh -o StrictHostKeyChecking=no ${user}@${host} "cd /var/www/u3390483/depalaw-api && pm2 restart ecosystem.config.json || echo 'PM2 restart failed, trying node directly...'"

expect {
    "password:" {
        send "${password}\r"
        exp_continue
    }
    eof
}
